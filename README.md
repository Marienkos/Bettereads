# Bettereads
Tired of using a well known platform to catalog my books, I decided to build my own. It started as a bunch of Markdown files with tables containing the info, but now I'm working on a SQL database (see Bettereads SQL).

# Stuff to do
Here is an ugly looking checklist of things I want to implement:
- Edit tables and adapt "add" parameters to it
- Automatically generate a Markdown showing the books after every update
- Polish the console interface
- Use a graphic library to build a native UI